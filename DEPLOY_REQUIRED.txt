部署到服务器时请随代码一并发布以下目录（与 webapp 同级，缺一不可）：

  web/static/       前端脚本与样式（Flask static_folder 指向此处）
  web/templates/    页面模板（优先于 webapp/templates 加载）

说明：应用入口在 webapp/__init__.py 中把静态资源与主模板目录绑定到项目根下的 web/。
若只同步 webapp/ 而遗漏 web/，会出现页面功能缺失、样式/脚本仍是旧版等问题。

提交到 Git 时请勿只提交 webapp/：请保证 web/ 的变更在同一提交或同一分支中一并推送。
