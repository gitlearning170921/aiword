# 可选 HTTPS 反代（profile nginx）
# 启用: docker compose -f deploy/docker-compose.yml --profile nginx up -d
# 证书: 将 fullchain.pem / privkey.pem 放入 deploy/nginx/certs/

upstream aiword_backend {
    server aiword:5000;
    keepalive 8;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 30m;

    location / {
        proxy_pass http://aiword_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 600s;
    }
}

# 若已放置 deploy/nginx/certs/fullchain.pem 与 privkey.pem，可取消下方注释并映射 443 端口
# server {
#     listen 443 ssl http2;
#     server_name aiword.yourdomain.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     client_max_body_size 30m;
#     location / {
#         proxy_pass http://aiword_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 600s;
#     }
# }
