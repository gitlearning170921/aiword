@echo off
rem 打包机本地配置（复制到 d:\aicode\build-machine.env.bat，勿提交 Git）
rem Docker Toolbox + D 盘 + Hub 慢时推荐：

set SKIP_DOCKER_JS_CHECK=1
set DOCKER_LEGACY_BUILD=1

rem Toolbox 建议 VirtualBox default 虚拟机内存 >= 4096MB，否则 apt-get 可能 runc 崩溃
